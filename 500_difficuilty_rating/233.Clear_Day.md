# Clear Day

Chef classifies a day to be either `rainy`, `cloudy`, or `clear`.

In a particular `week`, Chef finds `X` days to be `rainy` and `Y` days to be `cloudy`.
Find the number of `clear` days in the week.
## Input Format

- The first and only line of input will contain two space-separated integers `X` and `Y`, denoting the number of rainy and cloudy days in the week.
## Output Format

Output the number of clear days in the week.

## Constraints

- `0`≤`X`,`Y`≤`7`
- `0`≤`X`+`Y`≤`7`

## Sample 1

**Input**
```
2 3
```

**Output**
```
2
```

**Explanation:** There are `7` days in a week. if there are  `2` rainy days `3` cloudy dayss, then the remaining `7 - 2 - 3 = 2` days are clear.

## Sample 2

**Input**
```
3 4
```

**Output**
```
0
```

**Explanation:** If there are `3` rainy days and `4` cloudy days, then the remaining `7 - 3 - 4 = 0` days are clear.

## Solution

```javascript
const input = require('fs').readFileSync('/dev/stdin','utf8').trim();
const [x,y] = input.split(' ').map(Number);
console.log(7-(x+y))
```

## Explain Your Code

### Approach (Lines 9–14)

The approach is simple:
so first here we take incoming data in our input varaibles so here in input we get string of varaibles like this for example: `2 3` so we split that string and convert them into Number and add them into `x` and `y` varaibles so it will become `x = 2` and `y = 3` so now we add this two varaibles and then subtract it with `7` so we will get number of clear day `7 - ( x + y )`.